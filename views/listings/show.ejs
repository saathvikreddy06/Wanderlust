<% layout("/layouts/boilerplate.ejs") %>
<body>
    <h3 style="text-align: center;" class="listing-title"><%=listing.title%></h3>
    <div class="row">
        <div class="col-md-4 offset-md-4 col-12">
            <div class="card listing-card" style="width: 25rem;">
                <img src="<%=listing.image.url%>" class="card-img-top mb-2" alt="listing-image">
                <div class="card-body">
                    <p class="card-text"><%=listing.description%></p>
                </div>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">&#8377;<%=listing.price.toLocaleString("en-IN")%></li>
                    <li class="list-group-item"><%=listing.location%></li>
                    <li class="list-group-item"><%=listing.country%></li>
                    <div class="ms-3 mb-3 mt-2">Created by : <i><%=listing.owner.username%></i></div>
                </ul>
                <%if(currUser && listing.owner._id.equals(currUser._id)) {%>
                <div class="card-body box">
                    <a href="/listings/<%=listing._id%>/edit" class="card-link btn btn-dark mb-3">Edit</a>
                    <form method="post" action="/listings/<%=listing._id%>?_method=DELETE">
                        <button class="card-link btn btn-danger mb-3">Delete</button>
                    </form>
                </div>
                <%}%>
            </div>
        </div>
    </div>
    <hr>
    <% if(currUser) {%>
    <form class="mb-2 mt-2 needs-validation" action="/listings/<%=listing._id%>/reviews" method="post" novalidate>
        <h3>Add a review</h3>
        <label for="rating" class="form-label">Rating</label>
        <fieldset class="starability-slot">
            <input type="radio" id="no-rate" class="input-no-rate" name="review[rating]" value="0" checked aria-label="No rating." />
            <input type="radio" id="first-rate1" name="review[rating]" value="1" />
            <label for="first-rate1" title="Terrible">1 star</label>
            <input type="radio" id="first-rate2" name="review[rating]" value="2" />
            <label for="first-rate2" title="Not good">2 stars</label>
            <input type="radio" id="first-rate3" name="review[rating]" value="3" />
            <label for="first-rate3" title="Average">3 stars</label>
            <input type="radio" id="first-rate4" name="review[rating]" value="4" />
            <label for="first-rate4" title="Very good">4 stars</label>
            <input type="radio" id="first-rate5" name="review[rating]" value="5" />
            <label for="first-rate5" title="Amazing">5 stars</label>
        </fieldset>
        <div>
            <label for="comment" class="form-label">Comment</label>
            <textarea name="review[comment]" class="form-control" rows="5" required></textarea>
            <div class="invalid-feedback">Please add some comments to the review</div>
        </div>
        <button class="btn btn-outline-dark mt-2">Submit</button>
    </form>
    <hr>
    <% } %>
    <%if(listing.reviews.length>0) {%>
        <h3>All Reviews</h3>
        <div class="row">
            <%for(review of listing.reviews) {%>
                <div class="card ms-3 mb-3" style="width: 25rem;">
                    <div class="card-body">
                        <h5 class="card-title">@<%=review.author.username%></h5>
                        <p class="starability-result small-rating" data-rating="<%=review.rating%>"></p>
                        <p class="card-text"><%=review.comment%></p>
                    </div>
                    <%if(currUser && currUser.username==review.author.username) {%>
                        <form method="post" action="/listings/<%=listing._id%>/reviews/<%=review._id%>?_method=DELETE">
                            <button class="btn btn-dark btn-sm col-4 mb-3 mt-2">Delete</button>
                        </form>
                    <%}%>
                </div>
            <%}%>
        </div>
    <%}%>
</body>
